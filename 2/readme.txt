Чистый js был выбран, поскольку с DOM не происходит каких-то сложных взаимодействий и его полностью хватает + так страница будет загружаться несколько меньше 
Алгоритм следующий:
С помощью querySelector получаем наш select.
Вешаем функцию на событие change чтобы при изменении значения в select она выполнялась
Сама функция проверяет значения полей name и либо скрывает их либо показывает.
